# esbuild ships prebuilt platform binaries, so no dependency needs a build
# step here. Skipping them keeps installs deterministic and silences pnpm's
# strict "ignored builds" error in CI.
ignore-dep-scripts=true
